# Conditional AWS adapter implementation

Implement the approved AWS target locally without deploying or making paid calls. Own `infra/**`, `infra/tests/**`, `docs/deployment.md`. You may add cloud dependencies/groups to pyproject.toml and uv.lock. Do not touch frontend, scripts/evaluate.py or backend files except a narrowly necessary cloud-factory hook agreed with root. Do not spawn subagents or commit.

Read `backend/store.py`, `backend/worker.py`, `backend/app.py` interfaces. Supply a cloud repository compatible with `create_app(repository, start_worker=False)` and `process_run(repository, run_id)` sharing real domain code, not a second divergent workflow. Target DynamoDB, SQS durable events, AgentCore direct-code Python runtime, S3 session artifacts, EventBridge five-minute reminder checks; API Gateway+Lambda FastAPI via Mangum; S3 private origin+CloudFront UI via SAM.

Required correctness: conditional atomic reservation/transport state updates; deduped queue/idempotency claim with state; recovery if SQS publish or acknowledgement fails; serialize processing within sandbox/request; version/reset fencing; opaque hashed auth tokens and caller-only resets; no cloud calls on import unless cloud runtime entrypoint intentionally invoked. Limit sandbox size/request count for DDB item bounds if using whole documents, and explain the demo tradeoff. Avoid acquiring long-lived locks for API mutations while model calls block: queue API returns immediately; worker model effects only commit if relevant state still valid, with race/cost retry risks explicit.

Implement useful offline tests with moto or Stubber for repository conditional writes, isolation, reset/order/recovery and adapter entrypoints. Do not install AWS CLI or start deployment. Infrastructure defaults model spending OFF, protected judge access, explicit credits parameter, bounded concurrency/log retention/throttles, no broad public buckets. Conditional IAM resources should be least privilege practical for prototype. SAM/direct packaging may be marked unverified in AWS, but must match actual code paths, not nonfunctional placeholders called deployed.

AgentCore should run `backend.agent.coordinate` indirectly through shared worker and persist outcomes. S3 stores sanitized session/run artifacts without exposing private reasoning. Approval handoff is durable app decisions (current backend has no Strands interrupt/resume session; document honestly). EventBridge checks due reminders against scenario clock, not accidentally resetting fictional dates to real wall time.

Report in `docs/deployment.md`: commands, region/model/credits checks, resources, defaults, remaining AWS-only validation, tests, architecture compromises and exact handoff. No real accounts/credit evidence are available. Root is doing UI/docs/evaluation while you implement.
